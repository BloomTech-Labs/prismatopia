name: Lint

on: pull_request

jobs:
  lint:
    name: Run linters on PR
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@master
      - name: lint
        run: "yarn install"
        working-directory: apollo
      - name: lint
        run: "yarn run lint"
        working-directory: apollo
